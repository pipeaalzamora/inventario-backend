package ports

// Package ports contiene las interfaces de dominio destinadas exclusivamente a los adaptadores de datos.
// Aquí se definen los contratos que deben implementar los adaptadores para interactuar con las entidades del dominio.
// Los puertos representan las conexiones entre el dominio y las fuentes de datos, permitiendo una separación clara de responsabilidades.
// Estas interfaces son fundamentales para mantener la independencia del dominio respecto a las implementaciones concretas de

/*
ejemplo de puerto

type ExamplePort interface {
	//Los métodos que deben ser implementados por los adaptadores de datos
	// Ejemplo: GetByID, Save, Delete, etc.
}
*/
